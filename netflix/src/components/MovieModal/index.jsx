import React from 'react';
import "../MovieModal/MovieModal.css";

const MovieModal = ({backdrop_path, title, overview, name, release_date, vote_average, setModalOpen}) => {
    return (  
        <div>

        </div>
    );
}
 
export default MovieModal;